<%= form_with(model: [@genre, @book], local:true) do |f| %>
  <% if @book.errors.any? %>
    <div class="alert alert-warning">
      <h4 class="alert-heading"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h4>
      <ul>
        <% @book.errors.full_messages.each do |msg| %>
          <%= msg %>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
  <p>
  <div class="field">
    <%= f.label :TITLE, style: "display: block"  %>
    <%= f.text_field :title, class: "custom-text-field" %>
  </div>

  <br />
  <div class="field">
    <%= f.label :AUTHOR, style: "display: block"  %>
    <%= f.text_field :author, class: "custom-text-field" %>
  </div>
  <br />
 
  <div class="field">
    <%= f.label :SYNOPSIS, style: "display: block"  %>
    <%= f.text_area :synopsis, class: "custom-text-field" %>
  </div>

    <br />
  <div class="actions">
    <%= f.submit "SAVE", class: "btn btn-warning btn-sm" %>
  </div>
  </p>
  </div>
  
  <% end %>